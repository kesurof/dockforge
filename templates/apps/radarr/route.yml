http:
  routers:
    radarr:
      rule: "Host(`${APP_HOST}`)"
      entryPoints:
        - websecure
      service: radarr
      middlewares: ${TRAEFIK_PUBLIC_MIDDLEWARES}
      tls:
        certResolver: letsencrypt
  services:
    radarr:
      loadBalancer:
        servers:
          - url: http://radarr:7878
