http:
  routers:
    radarr:
      rule: "Host(`${APP_HOST}`)"
      entryPoints:
        - websecure
      service: radarr
${TRAEFIK_PUBLIC_MIDDLEWARES_BLOCK}
      tls:
        certResolver: letsencrypt
  services:
    radarr:
      loadBalancer:
        servers:
          - url: http://radarr:7878
