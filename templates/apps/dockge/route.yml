http:
  routers:
    dockge:
      rule: "Host(`${APP_HOST}`)"
      entryPoints:
        - websecure
      service: dockge
      middlewares: ${TRAEFIK_PUBLIC_MIDDLEWARES}
      tls:
        certResolver: letsencrypt
  services:
    dockge:
      loadBalancer:
        servers:
          - url: http://dockge:5001
