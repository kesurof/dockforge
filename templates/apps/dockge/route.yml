http:
  routers:
    dockge:
      rule: "Host(`${APP_HOST}`)"
      entryPoints:
        - websecure
      service: dockge
      tls:
        certResolver: letsencrypt
  services:
    dockge:
      loadBalancer:
        servers:
          - url: http://dockge:5001
